MARKETING_VERSION=0.1.7
BUILD_NUMBER=8
# Sparkle channel for generate_appcast. Empty = stable (default channel).
# Current builds are pre-release until a formal 1.x ships.
RELEASE_CHANNEL=pre-release
